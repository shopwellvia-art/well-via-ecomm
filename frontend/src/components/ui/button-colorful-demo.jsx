// Demo for ButtonColorful (ported from demo.tsx). The original demo imported
// `Button` and `LucideIcon` but never used them, so they're dropped here.
import { ButtonColorful } from "@/components/ui/button-colorful";

function ButtonDemo() {
    return <ButtonColorful />;
}

export { ButtonDemo };
