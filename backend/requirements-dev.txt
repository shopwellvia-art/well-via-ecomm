# Development / CI-only dependencies (test tooling).
#
# These are intentionally NOT in requirements.txt — the production image stays
# lean and never ships a test runner. CI installs this file into the already-
# built backend container (see .github/workflows/test.yml), and locally you can
# run `pip install -r requirements-dev.txt` inside the backend container the
# same way.
-r requirements.txt

pytest==8.3.4
